{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "SchematicsReactZustandStore",
  "title": "React Zustand Store Options Schema",
  "type": "object",
  "description": "生成React Zustand 集中式状态管理store",
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "description": "store名称",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "请输入你需要创建的store名称"
    }
  },
  "required": ["name"]
}
